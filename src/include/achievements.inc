// ==============================================================================================================================
// >>> Вызывается при чтении каждой секции в файле достижений
// ==============================================================================================================================
forward Achievements_OnConfigSectionReaded(Handle:hKeyValues, const String:sAchievementName[]);

// ==============================================================================================================================
// >>> Вызывается при получении клиентом достижения
// ==============================================================================================================================
forward Achievements_OnGotAchievement(iClient, const String:sAchievementName[]);

// ==============================================================================================================================
// >>> Функция, вызывающая обработку события. 
// >>> Вместо события Handle:event передается Handle:trie, 
// >>> которое необходимо заполнить по аналогии с событием.
// ==============================================================================================================================
native Achievements_ProcessEvent(iClient, Handle:hTrie, const String:sAchievementName[]);